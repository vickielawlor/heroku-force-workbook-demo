<table border="1" style="width:100%">
  <caption>Manager Information</caption>
  <tr><th>Start Time</th><th>Shown As</th><th>Shown As</th><th>Shown As</th></tr>
  <% @accounts.each do |account| %>
      <tr>
        <td><%= account.user.FirstName %></td>
        <td><%= account.user.LastName %></td>
        <td><%= account.user.MobilePhone %></td>
        <td><%= account.user.Email %></td>
      </tr>
  <% end %>
</table>

<table border="1" style="width:100%">
  <caption>Manager Information</caption>
  <tr><th>Start Time</th><th>Shown As</th><th>Shown As</th></tr>
  <% @accounts.each do |account| %>
      <tr>
        <td><%= account.u1.FirstName %></td>
      </tr>
  <% end %>
</table>